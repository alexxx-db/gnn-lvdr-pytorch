# Core dependencies — installed on Databricks clusters
# pyspark is provided by the runtime
dgl>=1.1
torch>=2.0
mlflow>=2.14
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
numpy>=1.24
pandas>=2.0
dbldatagen>=0.4

# Agentic + App
gradio>=4.0
databricks-sdk>=0.20

# Dev only
# pytest>=7.0
# ruff>=0.4
